using Drifter.Editor.Misc;
using UnityEditor;
using UnityEngine;

namespace Drifter.Editor
{
    public class DrifterInstaller : AssetPostprocessor
    {
        //private void OnPreprocessAsset()
        //{
        //    var importer = assetImporter;

        //    if (importer == null || !importer.importSettingsMissing)
        //        return;

        //    DrifterInstallerEditor.Init();
        //}
    }
}